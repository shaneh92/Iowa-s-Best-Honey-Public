export interface Product {
  id: string;
  name: string;
  originalPrice: number;
  salePrice: number;
  quantity: number;
  category: string;
  weight: number;
  description: string;
  image: string;
}
